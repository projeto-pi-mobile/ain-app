import { DefaultTheme } from "react-native-paper";


const theme = {
    ...DefaultTheme,
    colors: {
      ...DefaultTheme.colors,
      primary: "#8257E5",
      accent: "#04D361",
    },
  };

  export default theme;